/**
 * Shared selection chrome metrics (HTML toolbar placement helpers).
 * Kit paints product selection / path-edit handles.
 */

export const CHROME_STROKE_PX = 1.5;

/** Kit selection silhouette / handle stroke — keep HTML accents in sync. */
export const SELECTION_ACCENT_HEX = '#00A2FF';
